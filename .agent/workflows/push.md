---
description: 변경 사항을 Git에 자동으로 커밋하고 푸시합니다.
---

이 워크플로우는 모든 변경 사항을 스테이징하고, 메시지와 함께 커밋한 후 원격 저장소로 서버에 업로드합니다.

// turbo
1. 다음 명령어를 실행하여 Git에 푸시합니다:
```powershell
powershell -ExecutionPolicy Bypass -File .\push.ps1 "Agent automatic update"
```
