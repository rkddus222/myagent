# Vercel 배포용 최소 requirements.txt
# 핵심 의존성
python-dotenv==1.0.1
requests==2.31.0

# 웹 프레임워크
fastapi==0.115.0
mangum==0.18.0

# LangChain (최소한만)
langchain-core==0.3.66
langchain-openai==0.3.25
langchain-google-genai==2.0.10
langchain-perplexity==0.1.1

# AI 모델 API 클라이언트
openai==1.88.0
google-generativeai==0.8.4
perplexity-api==1.0.5

# 설정 관리
pydantic==2.11.7
pydantic-core==2.33.2
pydantic-settings==2.6.1

# 금융 데이터 (yfinance가 pandas/numpy를 자동으로 설치)
yfinance==0.2.49

# 검색 엔진
duckduckgo-search==8.1.1

# 유틸리티
tiktoken==0.9.0

# 제거된 의존성 (크기가 크거나 사용하지 않음):
# - uvicorn[standard] (서버리스에서는 불필요)
# - langchain (langchain-core로 충분)
# - langgraph, langgraph-*, langsmith (사용하지 않음)
# - pandas, numpy (yfinance가 자동 설치)
# - beautifulsoup4 (사용하지 않음)
# - selenium, webdriver-manager (사용하지 않음)
# - websocket-client (사용하지 않음)
